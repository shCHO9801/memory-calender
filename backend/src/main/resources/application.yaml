spring:
  application:
    name: backend

  datasource:
    url: jdbc:postgresql://localhost:5432/memory_calendar
    username: memory_calendar
    password: local_password

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    enabled: true